---
- name: Get system inventory
  hosts: localhost
#  connection: local
  gather_facts: False

  tasks:
  - name: Get system inventory
    dellemc_get_system_inventory:
       idrac_ip:   "{{ ip_idrac }}"
       idrac_user: "{{ user_idrac }}"
       idrac_pwd:  "{{ pwd_idrac }}"
